import { FileText, FolderPlus, Globe, PlaySquare } from "lucide-react";

export const iconMap: Record<string, any> = {
  file: FileText,
  video: PlaySquare,
  web: Globe,
  folder: FolderPlus,
  image: FileText,
  document: FileText,
};
